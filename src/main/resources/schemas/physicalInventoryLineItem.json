{
  "type": "object",
  "$schema": "http://json-schema.org/draft-04/schema",
  "description": "Physical inventory line item",
  "properties": {
    "orderable": {
      "type": "object",
      "title": "Orderable",
      "description": "Indicates which orderable this physical inventory line item is for.",
      "required": true,
      "$ref": "orderable"
    },
    "quantity": {
      "type": "integer",
      "description": "Indicates stock on hand for an orderable.",
      "required": true
    }
  }
}
