{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "title": "StockEventHistoryDto",
  "description": "A single transaction history row (issue, receive or adjustment stock event).",
  "properties": {
    "id": {
      "type": "string",
      "description": "Stock event id."
    },
    "documentNumber": {
      "type": ["string", "null"],
      "description": "Generated document number of the event."
    },
    "type": {
      "type": "string",
      "enum": ["ISSUE", "RECEIVE", "ADJUSTMENT"],
      "description": "Transaction type, read from the event origin."
    },
    "signature": {
      "type": ["string", "null"],
      "description": "Signature of the user who performed the event."
    },
    "occurredDate": {
      "type": ["string", "null"],
      "description": "Earliest occurred date across the event's line items (ISO date)."
    },
    "processedDate": {
      "type": ["string", "null"],
      "description": "Date and time the stock event was processed (ISO date-time with zone)."
    },
    "entriesCount": {
      "type": ["integer", "null"],
      "description": "Count of stock event line item entries in the event."
    },
    "userId": {
      "type": ["string", "null"],
      "description": "Id of the user who performed the transaction."
    },
    "username": {
      "type": ["string", "null"],
      "description": "Resolved username of the user who performed the transaction."
    },
    "facilityId": {
      "type": ["string", "null"],
      "description": "Facility the event was recorded for. The transaction detail view resolves the cancel permission and the current stock on hand from it."
    },
    "programId": {
      "type": ["string", "null"],
      "description": "Program the event was recorded for. The transaction detail view resolves the cancel permission and the current stock on hand from it."
    },
    "reversible": {
      "type": ["boolean", "null"],
      "description": "Whether the event holds at least one line item that does not reverse another one, i.e. whether anything in it can still be cancelled."
    }
  },
  "required": ["id", "type"]
}
